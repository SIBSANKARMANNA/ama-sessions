## Which command is used to view Git history?
- git log
## What is border-box and box-sizing in CSS?
- box-sizing: border-box makes the declared width and height include content + padding + border.
## How can you find the IP address using the CLI?
- hostname -I
## What is the purpose of the top command in the CLI?
- It displays running processes and system resource usage such as CPU and memory.
## What is the difference between git pull and git fetch?
- fetch : Downloads changes but does not merge them.
- pull : Downloads changes and merges them.
## What is the purpose of git add?
- Moves changes to the staging area before committing.
## What are the different methods of list in Python?
- append()
- extend()
- pop()
- insert()
- remove()
- index()
## How can you select a particular element in a list without using an ID selector?
- Using pseudo class. li:nth-child(3)
## What is the difference between COMMIT and ROLLBACK in SQL?
- COMMIT : Permanently saves the transaction changes.
- ROLLBACK : Undoes uncommitted changes.

## What is multithreading in Python?
- Multithreading allows multiple threads to execute tasks concurrently within a program.
## How can you get the top 30 lines from a particular line?
- head -n 30 
## What are Decorator in python?
- A decorator adds or modifies the behavior of a function without changing its original code.